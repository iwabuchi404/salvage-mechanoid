R6レビュー指摘を修正する

ユーザーレビューで指摘された R6 の境界漏れと堅牢性の問題を修正した。

主な変更:

1. VisibilityRule: 隣接タイル代理判定を廃止し現在タイルのみで可視性判定
   - 隣接マスを代理にすると壁が可視のとき壁の背後の Entity まで可視になる問題を修正
   - 移動補間中も丸めた現在位置だけを判定する

2. FOVSystem: フロア別探索状態と destroy 実装
   - exploredTilesByFloor で探索済みタイルをフロア単位で保持
   - floor_changed イベント購読でフロア切替時に差分状態をリセットして再計算
   - activateFloor() でフロア別の探索状態へ切り替え
   - move_completed/fov_update_requested/floor_changed のリスナーをフィールド保持し
     destroy() で確実に解除
   - reset() でフロア別探索状態もクリア

3. InteractionQuery: 距離範囲と active チェックを追加
   - InteractionQueryOptions で minRange/maxRange（マンハッタン距離）を指定可能
   - 既定は同一マスのみ（maxRange=0）で後方互換
   - 非 active の Entity は候補から除外

4. InteractionExecutor: 実行時に entity.active を再チェック
   - 候補抽出後に非 active になった場合の保護

5. テストを追加（+8件）
   - FOVSystem.spec.ts: 3件（フロア切替の再通知・探索状態の分離・destroy の購読解除）
   - InteractionQuery: 3件（非 active 除外・maxRange=1・minRange=1）
   - InteractionExecutor: 2件（候補抽出後の非 active・直接指定の非 active）

検証:
- ユニットテスト: 41 スイート、514 件（全通過）
- 型検査・ビルド成功
- Playwright E2E: 5 件成功

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
